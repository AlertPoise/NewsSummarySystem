"""BERT 句子语义编码接口。"""


class BertEncoder:
    """只加载一次的 BERT 批量句向量编码器。"""

    # TODO(C-阶段2)：实现 Hugging Face 模型单次加载、GPU 选择和 inference_mode 推理；输入为 B 确认 checkpoint 与句子列表，输出为每句一个语义向量，必须遵守 docs/AI_PIPELINE.md 的 BERT 步骤。
    pass
