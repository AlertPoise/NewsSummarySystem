"""CNewSum 数据准备入口。"""


def main() -> None:
    """准备正式训练、验证和测试数据。"""
    # TODO(B-阶段2)：仅从 runtime/datasets/CNewSum_v2/final/ 读取真实 CNewSum，先验证字段、编码和划分，再生成 train、validation、test 和统计信息；输入为真实数据，输出为每条含 id、article、summary 的标准样本，必须遵守 model_training/config.yaml 与 docs/ARCHITECTURE.md。
    raise NotImplementedError("阶段2由B处理CNewSum")


if __name__ == "__main__":
    main()
