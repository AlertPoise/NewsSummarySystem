"""CNewSum 正式模型评价入口。"""


def main() -> None:
    """在 CNewSum test 上计算 ROUGE 指标。"""
    # TODO(B-阶段2)：对 CNewSum test 的真实生成结果计算 ROUGE-1、ROUGE-2、ROUGE-L；输入为正式模型和测试集参考摘要，输出为可写入 model_evaluations 的真实指标且 ROUGE-L 不低于 0.40，必须遵守 docs/TEST_PLAN.md。
    raise NotImplementedError("阶段2由B进行真实模型评价")


if __name__ == "__main__":
    main()
