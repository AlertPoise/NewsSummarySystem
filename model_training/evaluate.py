"""CNewSum 正式模型评价入口。"""


def main() -> None:
    """在 CNewSum test 上计算 ROUGE 指标。"""
    # TODO(B-阶段2)：在 C2-13 提供的完整正式 Pipeline 上，对 CNewSum test 的真实生成结果计算 ROUGE-1、ROUGE-2、ROUGE-L 与质量达标率；输入为正式模型、完整 Pipeline 和测试集参考摘要，输出为可写入 model_evaluations 的真实指标，必须遵守 docs/ARCHITECTURE.md 与 docs/REQUIREMENTS.md。
    raise NotImplementedError("阶段2由B进行真实模型评价")


if __name__ == "__main__":
    main()
