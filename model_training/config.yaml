# TODO(B-阶段2)：由 B 根据真实 CNewSum 数据规模、正式 checkpoint 和硬件验证填写 CHANGE_ME；输入为训练与验证实验记录，输出为唯一正式训练配置，必须遵守 docs/AI_PIPELINE.md 和 runtime/models/news_summarizer/ 保存约定。
dataset:
  name: CNewSum
  directory: ../runtime/datasets
  train_file: CHANGE_ME
  validation_file: CHANGE_ME
  test_file: CHANGE_ME
model:
  checkpoint: CHANGE_ME
  output_directory: ../runtime/models/news_summarizer
  version: CHANGE_ME
training:
  seed: CHANGE_ME
  num_train_epochs: CHANGE_ME
  learning_rate: CHANGE_ME
  per_device_train_batch_size: CHANGE_ME
  per_device_eval_batch_size: CHANGE_ME
  max_source_length: CHANGE_ME
  max_target_length: CHANGE_ME
evaluation:
  rouge_metrics: [rouge1, rouge2, rougeL]
benchmark:
  batch_size: 1
  warmup_runs: CHANGE_ME
  measured_runs: CHANGE_ME
